{
 "_meta": {
  "what": "THE TEST REGISTRY for knowledge/addon-dev/ — the id ⇄ ns.Test{} ⇄ run-row join, keyed by a stable id. It is NOT where an unknown is first written down: that is a MARKER on the claim itself, in the topic file (`[gap]` / `[unverified]` / `@verify-ingame`), because an agent reading the file meets the marker in the prose it is already reading. A question earns a row here when somebody decides to TEST it. Rows also exist for questions nobody is testing (`parked`, `not-answerable`) where the reasoning was worth keeping — but the absence of a row never means an unknown was lost.",
  "why_here": "knowledge/addon-dev/ is firewalled from the game KB (its README §0), so its `@verify-ingame` markers are written inside backticks and `wowkb.gen_verify` ignores them — a DECISION, not a bug (addon-dev/README.md §6, gen_verify.py's docstring). Nothing harvests them, and nothing needs to: the marker is met at the point of USE, by whoever is about to build on the claim. This file exists for the half a marker cannot do — binding a question to the Lua that tests it and the run row that answers it. Indexed from addon-dev/README.md §1.2 as the fourth queue.",
  "id_is_the_key": "The join column between this file, the `id` on each ns.Test{} in ClientLab/T_*.lua, and each row of the `runs` capture stream (`ClientLabDB.captures.runs`, read by `wowkb.capture clab runs` / `wowkb.lab show`). An id survives the anchor line moving; anchor text does not.",
  "expect_never_ships": "`expect` lives HERE and is never compared in-game. cdmp asserts a known answer against regression; the lab discovers an unknown one, so the reader renders result beside expect and a HUMAN decides. An automatic PASS/FAIL would be the instrument grading its own subject.",
  "status": {
   "answered": "flown, read beside `expect` by a human, and drained into the KB. Carries an `answered` block naming the run, the addon version and the OBS-nnn that carried it. ITS TEST IS DELETED — in the same edit that writes the claim, exactly as house rule 2 kills probe code once its finding lands. The row survives forever and is the recipe if a patch ever makes the question worth re-asking; the Lua does not, because nothing re-checks it (`show` prints no verdict by design and `drain` refuses an answered question), so a retained test only costs run time on every pull.",
   "built": "a ns.Test{} with this id exists in ClientLab/ and flies on the next login/pull — Autorun runs every built test unprompted, so writing the test IS queuing it. The claim it answers carries `@pending-test: <id>` in its topic file. If it has been flown but NOT drained, a `note` says why — an undrained flown question must never look the same as an unflown one.",
   "parked": "a question somebody wrote out but nobody is testing. No test, no clock, no cap, no release gate. `blocked_on` says what it would take (null = nothing, it has simply not come up). The trigger for building a test is USE — someone needs the answer to write code — never age. ⚠ A row here is not required for an unknown to be recorded: the marker on the claim is, and a bare marker with no row is a perfectly good parked question.",
   "not-answerable": "no instrument can settle it and the reason is recorded in `expect`, so it is not re-attempted. `blocked_on` may still name what a DIFFERENT instrument would need — a desktop file read is not a ClientLab job, but it is not nothing either."
  },
  "cross_check": "`wowkb.lab deploy` asserts every `built` id here has a ns.Test{} and every ns.Test{} id is a `built` question. An unmatched id is a loud error, never a silent skip. An `answered` id still present in the Lua fails BY NAME — that is the direction the suite grows back in, and it is the one worth naming.",
  "counts_as_of": "2026-08-05 (the unknowns pass + the drain that followed it): 51 answered, 6 built, 17 parked, 3 not-answerable (total 77). The six statuses collapsed to four here — `deferred`, `deferred-w1d` and `candidate` all said 'nobody got to it', which is not a state worth three names. `wowkb.lab deploy --check` refuses a status outside the four. The 6 remaining `built` rows are the whole live suite: 3 declare `blocked_on` and can only decline, 2 need a game situation nobody has been in (a CallbackEvent firing, a hero-tree swap), and 1 is the aspect-less eyeball channel.",
  "anchor_form": "<file under knowledge/addon-dev/>:<line>. `also` lists further anchors that ask the same question — one test, several places in the KB to edit when it is answered.",
  "anchor_drift": "Anchors are `<file>:<line>` and LINE NUMBERS MOVE — the KB cleanup of 2026-08-05 silently drifted several. The `id` is the durable key; an anchor is a convenience. Treat a stale anchor as a lint finding, not as evidence the question is gone. ⚠ Nothing validates anchors today; that check belongs in `wowkb.lab deploy --check`.",
  "note_field": "Free text on a question that has been flown but not drained, or is otherwise not in the state its `status` alone suggests. It exists so 'we looked and chose not to' is distinguishable from 'nobody has looked'.",
  "evidence_classes": "A row on the `runs` stream carries `source`: \"measured\" (an instrument read it) or \"eyeball\" (a human answered after being shown a stimulus by ns.Ask). They are NOT interchangeable. An eyeball verdict is the only evidence that can close a question with no programmatic oracle, and the WRONG evidence for anything measurable — so `wowkb.lab show` renders it differently and `wowkb.lab drain` requires it for a visual question and labels it loudly in the observation it mints.",
  "blocked_on": "What the question is waiting on, as a CAPABILITY rather than a task: many rows share one blocker, and `wowkb.lab blocked` groups them so the residue reads as a handful of missing capabilities instead of a pile of stuck questions. `null` means nothing is missing. Two blockers need their jargon spelled out: 'a generator that writes sibling addons' — ClientLab cannot test its own .toc from inside its own .toc without breaking itself, so every container question needs N generated throwaway addons, one per .toc variant (unbuilt, and not worth building until a .toc problem actually bites); and 'XML authoring in the lab' — the lab ships Lua only, so nothing can currently exercise the XML compiler.",
  "two_things_that_may_never_accumulate": "A MEASURED ANSWER NOT YET IN THE KB — gated by `wowkb.obs check` (14 days / 12 open), the failure that once stranded 15 answers for 12 days. And AN UNVERIFIED CLAIM SILENTLY RELIED UPON — gated by nothing mechanical, by design: the marker sits on the claim, so anyone reading the claim reads the marker, and the rule is ASK rather than assume. Everything else — parked questions, [gap] annotations — may pile up harmlessly. They are a CATALOGUE, not a backlog.",
  "marker_lifecycle": "The state of a question lives in the TOPIC FILE, on the claim: `[gap]`/`[unverified]`/`@verify-ingame` = open, nobody is on it. `@pending-test: <id>` = a ns.Test{} exists in ClientLab and will fly on the next login/pull — the id names the row here. `[client YYYY-MM-DD]` = measured and drained, marker gone. Promotion to `@pending-test` and the final rewrite are hand edits, in context, because README §7.1 wants the CLAIM rewritten rather than annotated — no tool edits prose.",
  "shrinking": "THE SUITE SHRINKS AS THE KB GROWS, and that is the intended shape. Answering a question moves it out of ClientLab and into a topic file: 32 answered questions were swept out at once when this rule was adopted, taking the suite from 57 ids to 25 and deleting T_Module.lua and T_CooldownManager.lua entirely. A lab that only accumulates is re-measuring settled facts during a pull."
 },
 "questions": [
  {
   "id": "addon-exists-nested-lib",
   "anchor": "anatomy-and-runtime.md:127",
   "also": [
    "anatomy-and-runtime.md:152",
    "anatomy-and-runtime.md:1165"
   ],
   "bucket": "call",
   "status": "answered",
   "question": "Is a nested-`.toc` library (LibStub) enumerated by the client as an addon in its own right? And is a plainly-installed top-level addon (RaiderIO) reported as existing even though it is missing from AddOns.txt?",
   "expect": "DoesAddOnExist(\"LibStub\") == false and DoesAddOnExist(\"RaiderIO\") == true. That combination confirms the §19b folder-name rule AND kills the draft's inference that absence from AddOns.txt proves never-enumerated. Either half coming back the other way rewrites §1.",
   "answered": {
    "run": "2026-07-24 07:49:13",
    "version": "0.1.0",
    "obs": "OBS-008",
    "tag": "[client 2026-07-24]"
   }
  },
  {
   "id": "sandbox-require-os-io",
   "anchor": "anatomy-and-runtime.md:677",
   "bucket": "call",
   "status": "answered",
   "question": "Does the addon sandbox expose `require`, `os` or `io`?",
   "expect": "All three nil. 2298 shipped .lua files contain zero uses of require/dofile/loadfile/module/os./io., which is corroboration by absence; this converts it to a measurement. A non-nil `os` would be a notable finding worth its own KB section.",
   "answered": {
    "run": "2026-07-24 07:49:13",
    "version": "0.1.0",
    "obs": "OBS-005",
    "tag": "[client 2026-07-24]"
   }
  },
  {
   "id": "string-rtgsub-callable",
   "anchor": "anatomy-and-runtime.md:690",
   "bucket": "call",
   "status": "answered",
   "question": "Is `string.rtgsub` — wiki-tagged `framexml`, i.e. flagged Blizzard-internal rather than addon-facing — actually callable from addon code?",
   "expect": "Unknown; that is the point. If the `framexml` tag means anything enforceable, the lookup is nil or the call errors from an insecure frame. If it is merely documentation of intent, it is a normal function and works.",
   "answered": {
    "run": "2026-07-24 07:49:13",
    "version": "0.1.0",
    "obs": "OBS-006",
    "tag": "[client 2026-07-24]"
   }
  },
  {
   "id": "wow-lua-zero-use-fns",
   "anchor": "anatomy-and-runtime.md:697",
   "bucket": "call",
   "status": "answered",
   "question": "Do `table.freeze`, `table.isfrozen`, `table.removemulti` and `strsplittable` exist at 12.0.7? All four are wiki-listed with ZERO uses in the shipped source, so they rest on Tier 2 alone.",
   "expect": "All four present as functions. If any is nil, the wiki list is stale and §5's WoW-additions table needs a correction — which is exactly the failure mode a zero-use, Tier-2-only claim has.",
   "answered": {
    "run": "2026-07-24 07:49:13",
    "version": "0.1.0",
    "obs": "OBS-007",
    "tag": "[client 2026-07-24]"
   }
  },
  {
   "id": "issecure-in-addon-frame",
   "anchor": "anatomy-and-runtime.md:768",
   "also": [
    "anatomy-and-runtime.md:1212"
   ],
   "bucket": "call",
   "status": "answered",
   "question": "Does `issecure()` ever return true in an addon call frame — i.e. can an addon satisfy the `assert(issecure())` guarding `AddLuaErrorHandler`?",
   "expect": "false, from a plain addon call frame. Rule 23 currently states the consequence ('no addon may register a Lua error handler this way') as inference from the assert; this measures the premise. Also probe whether AddLuaErrorHandler is even visible to us.",
   "note": "NOT DRAINED from the 2026-07-24 run, deliberately. The run recorded issecureResult=\"errored: false\", which is AMBIGUOUS: T_Anatomy.lua used `ok and (res and true or false) or (\"errored: \"..res)`, and a legitimate `issecure() == false` makes the middle term falsy so Lua's `or` falls through to the ERROR branch. false is the expected answer, so the one result the test exists to record is the one it cannot spell. The expression was fixed 2026-08-05 to branch on `ok` with an if; RE-FLY to get an unambiguous value. The AddLuaErrorHandler=function half was clean but is not on its own worth a KB edit.",
   "answered": {
    "run": "2026-08-05 14:31:51",
    "version": "0.2.0",
    "obs": "OBS-037",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "combatlog-secure-globals",
   "anchor": "api-events-and-discovery.md:484",
   "bucket": "call",
   "status": "answered",
   "question": "Do the six `C_CombatLog.*` names that are documented only on `C_CombatLogSecure` (AddEventFilter, ClearEventFilters, GetCurrentEntryInfo, GetCurrentEventInfo, GetEntryCount, ShouldShowCurrentEntry) resolve to nil for an addon?",
   "expect": "All six nil on `C_CombatLog` for us — the `Environment = \"SecureOnly\"` reading. Any that is non-nil means SecureOnly is not enforced by nil-ing the binding, and §3's account of the combat-log surface changes.",
   "answered": {
    "run": "2026-07-24 07:49:13",
    "version": "0.1.0",
    "obs": "OBS-012",
    "tag": "[client 2026-07-24]"
   }
  },
  {
   "id": "is-callback-event",
   "anchor": "api-events-and-discovery.md:411",
   "bucket": "call",
   "status": "answered",
   "question": "Does `C_EventUtils.IsCallbackEvent` return true for exactly the 12 `CallbackEvent = true` events? Call it on a flagged event and on a non-flagged one.",
   "expect": "true for a flagged event (e.g. MINIMAP_PING), false for an unflagged one (e.g. PLAYER_LOGIN). That would upgrade 'the 12 flagged events are exactly what RegisterEventCallback accepts' from a reasonable reading to a measurement. The function is called nowhere in the shipped Lua, so its behaviour is entirely unexercised.",
   "answered": {
    "run": "2026-07-24 07:49:13",
    "version": "0.1.0",
    "obs": "OBS-011",
    "tag": "[client 2026-07-24]"
   }
  },
  {
   "id": "unregister-event-callback",
   "anchor": "api-events-and-discovery.md:389",
   "bucket": "call",
   "status": "answered",
   "question": "Does `Frame:UnregisterEventCallback` exist, and what is its arity? It has exactly one call site in the whole checkout (FrameUtil.lua:59), no generated doc entry, no wiki page, and is absent from the 12.0.0 added-widget list.",
   "expect": "Present as a function on a frame (FrameUtil calls it, so it must be, unless FrameUtil path is dead). Existence is the cheap half; arity needs a call and is recorded as whatever the error says.",
   "answered": {
    "run": "2026-07-24 07:49:13",
    "version": "0.1.0",
    "obs": "OBS-010",
    "tag": "[client 2026-07-24]"
   }
  },
  {
   "id": "event-profiler-api",
   "anchor": "api-events-and-discovery.md:893",
   "bucket": "call",
   "status": "answered",
   "question": "Do `GetCurrentEventID()` and `GetEventTime(eventProfileIndex)` exist and return anything useful when called from an addon? Neither is called anywhere in the 2298 shipped .lua files, so there is no worked example of obtaining a valid eventProfileIndex.",
   "expect": "Both exist (they are in FrameScriptDocumentation). GetCurrentEventID called outside a handler probably returns nil or 0; GetEventTime with that value probably errors or returns nothing. Recording the error IS the answer — it is what tells a future reader the pair is unusable without a profiling CVar.",
   "answered": {
    "run": "2026-07-24 07:49:13",
    "version": "0.1.0",
    "obs": "OBS-013",
    "tag": "[client 2026-07-24]"
   }
  },
  {
   "id": "register-unit-event-cap",
   "anchor": "api-events-and-discovery.md:200",
   "bucket": "call",
   "status": "answered",
   "question": "Is `RegisterUnitEvent`'s documented four-unit cap real? The generated docs express the unit parameter as an unbounded variadic; the four-unit limit is Tier 2 only.",
   "expect": "Passing 5 units errors, or silently ignores units past the fourth. Distinguish the two: after registering with 5, the test records whether the call raised. If it neither errors nor is observably capped, the Tier-2 cap claim should be softened in §2.2.",
   "answered": {
    "run": "2026-07-24 07:49:13",
    "version": "0.1.0",
    "obs": "OBS-009",
    "tag": "[client 2026-07-24]"
   }
  },
  {
   "id": "secure-hooks-allowed-flag",
   "anchor": "api-events-and-discovery.md:959",
   "also": [
    "module-architecture.md:1089"
   ],
   "bucket": "call",
   "status": "answered",
   "question": "What does the `SecureHooksAllowed = false` annotation actually do? `hooksecurefunc` a function carrying it (`CreateFromMixins`, `Mixin`) and see whether the hook is refused.",
   "expect": "Unknown and genuinely open. The annotation appears in 24 generated doc entries and in zero Lua logic anywhere in the shipped source, and it overlaps the wiki's empirically-derived unhookable list on only ONE name — which is a reason for caution, not confidence. If the hook installs without error, the natural reading ('hooking these raises') is wrong and both §2.4 and module-architecture rule 6 need rewriting.",
   "answered": {
    "run": "2026-07-24 07:49:13",
    "version": "0.1.0",
    "obs": "OBS-014",
    "tag": "[client 2026-07-24]"
   }
  },
  {
   "id": "dropdown-menu-globals",
   "anchor": "libraries-and-ecosystem.md:552",
   "also": [
    "libraries-and-ecosystem.md:789"
   ],
   "bucket": "call",
   "status": "answered",
   "question": "Do the deprecated `UIDropDownMenu_*` globals still resolve for addon code at 12.0.7? UIDropDownMenu.lua opens with `local envTable = GetCurrentEnvironment()` and resolves through `envTable[...]` rather than `_G`.",
   "expect": "This is the question `ns.GlobalType` exists for: a name may be absent from `_G` yet present in the addon environment, or vice versa. If `_G.UIDropDownMenu_Initialize` is nil, the file is scoped away from addons and LibUIDropDownMenu's continued use by WeakAurasOptions in 2026 gets a causal explanation the KB currently declines to assert.",
   "answered": {
    "run": "2026-07-24 07:49:13",
    "version": "0.1.0",
    "obs": "OBS-016",
    "tag": "[client 2026-07-24]"
   }
  },
  {
   "id": "create-from-mixins-nil",
   "anchor": "module-architecture.md:104",
   "bucket": "call",
   "status": "answered",
   "question": "Does `CreateFromMixins(nil)` error, or silently produce an empty mixin? This is what happens if a .toc lists a mixin file after its consumer.",
   "expect": "Errors. The Lua SecureMixin implementation does `for k, v in pairs(mixin)`, which raises on nil — but Mixin/CreateFromMixins are engine functions whose failure mode has never been observed, so the Lua reading may not describe the C one. A silent empty table would be a much nastier failure mode and worth calling out in §1.2.",
   "answered": {
    "run": "2026-07-24 07:49:13",
    "version": "0.1.0",
    "obs": "OBS-015",
    "tag": "[client 2026-07-24]"
   }
  },
  {
   "id": "secret-api-surface",
   "anchor": "security-taint-and-restricted-data.md:1328",
   "bucket": "call",
   "status": "answered",
   "question": "Which of the security primitives are actually visible to an addon? `issecure`, `issecurevariable`, `issecretvalue`, `issecrettable`, `canaccessvalue`, `secureexecuterange`, `forceinsecure`, `hooksecurefunc`, `C_Secrets`. All are absent from the generated docs, and `issecurevariable` appears nowhere in the shipped source either — the wiki is its only source at any tier.",
   "expect": "All present as functions (CDMProbe already calls issecretvalue/issecrettable/canaccessvalue successfully). `issecurevariable` is the one to watch: a single Tier-2 source with zero shipped call sites is exactly the shape of a claim that turns out to be stale. This test is also the PRECONDITION for the whole §4.2 table below — if issecretvalue is missing, Secret.lua cannot gate anything.",
   "answered": {
    "run": "2026-07-24 07:49:13",
    "version": "0.1.0",
    "obs": "OBS-018",
    "tag": "[client 2026-07-24]"
   }
  },
  {
   "id": "addon-message-prefix-limit",
   "anchor": "state-persistence-and-communication.md:935",
   "bucket": "call",
   "status": "answered",
   "question": "Is the 16-character addon-message prefix limit real, and how is it enforced? Register a 16-char prefix and a 17-char prefix and record both return values.",
   "expect": "16 succeeds, 17 returns false (or errors). The generated docs type the parameter as a bare `cstring` with no limit; wiki (Tier 2) + AceComm's tested comment (Tier 3) agree on 16, which is the strongest corroboration available and still not Tier 1. Harvested from a bare [gap] with no marker — see _meta.status.candidate — and promoted to built because it is pure call-and-record.",
   "answered": {
    "run": "2026-07-24 07:49:13",
    "version": "0.1.0",
    "obs": "OBS-017",
    "tag": "[client 2026-07-24]"
   }
  },
  {
   "id": "secret-op-store-local",
   "anchor": "security-taint-and-restricted-data.md:722",
   "bucket": "secret",
   "status": "answered",
   "question": "§4.2 row 1 — store a secret in a local / upvalue / table VALUE.",
   "expect": "Allowed. If this errors, nothing else in the table is testable and the whole secret-handling chapter is wrong.",
   "answered": {
    "run": "2026-08-05 13:59:03",
    "version": "0.2.0",
    "obs": "OBS-021",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "secret-op-pass-lua-fn",
   "anchor": "security-taint-and-restricted-data.md:723",
   "bucket": "secret",
   "status": "answered",
   "question": "§4.2 row 2 — pass a secret to a Lua function.",
   "expect": "Allowed. This is the premise of every 'launder it through a helper' pattern.",
   "answered": {
    "run": "2026-08-05 13:59:03",
    "version": "0.2.0",
    "obs": "OBS-022",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "secret-op-pass-c-fn",
   "anchor": "security-taint-and-restricted-data.md:724",
   "bucket": "secret",
   "status": "answered",
   "question": "§4.2 row 3 — pass a secret to an unmarked C function (e.g. `strlen`-ish builtin / a C API not marked as secret-accepting).",
   "expect": "Errors, unless the API is explicitly marked (§4.5). Which C function is chosen matters: the test uses one with no marking so a pass would be genuinely surprising.",
   "answered": {
    "run": "2026-08-05 13:59:03",
    "version": "0.2.0",
    "obs": "OBS-023",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "secret-op-concat",
   "anchor": "security-taint-and-restricted-data.md:725",
   "bucket": "secret",
   "status": "answered",
   "question": "§4.2 row 4 — concatenate a secret string/number with `..`.",
   "expect": "Allowed, and the RESULT is itself secret. The table says allowed but says nothing about the result's secrecy — record `issecretvalue` on the result, which is the half the KB does not state.",
   "answered": {
    "run": "2026-08-05 13:59:03",
    "version": "0.2.0",
    "obs": "OBS-024",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "secret-op-string-format",
   "anchor": "security-taint-and-restricted-data.md:726",
   "bucket": "secret",
   "status": "answered",
   "question": "§4.2 row 5 — `string.format` / `string.join` / `string.concat` with a secret argument.",
   "expect": "Allowed, result secret. Same as above: the secrecy of the OUTPUT is the interesting half, since it decides whether a formatted string can be printed.",
   "answered": {
    "run": "2026-08-05 13:59:03",
    "version": "0.2.0",
    "obs": "OBS-025",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "secret-op-arith",
   "anchor": "security-taint-and-restricted-data.md:727",
   "bucket": "secret",
   "status": "answered",
   "question": "§4.2 row 6 — arithmetic on a secret number.",
   "expect": "Errors. This is the row CDMProbe's HUD is designed around — no cooldown math on a secret.",
   "answered": {
    "run": "2026-08-05 13:59:03",
    "version": "0.2.0",
    "obs": "OBS-026",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "secret-op-compare",
   "anchor": "security-taint-and-restricted-data.md:728",
   "bucket": "secret",
   "status": "answered",
   "question": "§4.2 row 7 — compare a secret with `==` and with `<`.",
   "expect": "Errors. Test BOTH operators separately: `==` on differing types is a plausible engine special case and the table does not distinguish them.",
   "answered": {
    "run": "2026-08-05 13:59:03",
    "version": "0.2.0",
    "obs": "OBS-027",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "secret-op-bool-test-boolean",
   "anchor": "security-taint-and-restricted-data.md:729",
   "bucket": "secret",
   "status": "built",
   "question": "§4.2 row 8 — boolean test (`if v then`) on a BOOLEAN secret.",
   "expect": "Errors. Needs a secret whose real type is boolean, which the cooldown source may not provide — if so this records `skipped` with that reason, never a value.",
   "blocked_on": "a boolean-valued secret source"
  },
  {
   "id": "secret-op-bool-test-nonboolean",
   "anchor": "security-taint-and-restricted-data.md:730",
   "bucket": "secret",
   "status": "answered",
   "question": "§4.2 row 9 — boolean test on a NON-boolean secret.",
   "expect": "Allowed — type isn't secret, so nil→false and everything else→true. The asymmetry with row 8 is the single subtlest rule in the chapter and the one most likely to be misremembered.",
   "answered": {
    "run": "2026-08-05 13:59:03",
    "version": "0.2.0",
    "obs": "OBS-028",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "secret-op-length",
   "anchor": "security-taint-and-restricted-data.md:731",
   "bucket": "secret",
   "status": "answered",
   "question": "§4.2 row 10 — length operator `#` on a secret.",
   "expect": "Errors.",
   "answered": {
    "run": "2026-08-05 13:59:03",
    "version": "0.2.0",
    "obs": "OBS-029",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "secret-op-table-key",
   "anchor": "security-taint-and-restricted-data.md:732",
   "bucket": "secret",
   "status": "answered",
   "question": "§4.2 row 11 — use a secret as a table KEY.",
   "expect": "Errors. Contrast with row 1 (as a table VALUE, allowed) — the key/value asymmetry is the rule that bites caching code.",
   "answered": {
    "run": "2026-08-05 13:59:03",
    "version": "0.2.0",
    "obs": "OBS-030",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "secret-op-index",
   "anchor": "security-taint-and-restricted-data.md:733",
   "bucket": "secret",
   "status": "answered",
   "question": "§4.2 row 12 — index and index-assign a secret (`secret.foo`, `secret[\"foo\"] = 1`).",
   "expect": "Both error. Test read and write separately.",
   "answered": {
    "run": "2026-08-05 13:59:03",
    "version": "0.2.0",
    "obs": "OBS-031",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "secret-op-call",
   "anchor": "security-taint-and-restricted-data.md:734",
   "bucket": "secret",
   "status": "answered",
   "question": "§4.2 row 13 — call a secret as a function.",
   "expect": "Errors.",
   "answered": {
    "run": "2026-08-05 13:59:03",
    "version": "0.2.0",
    "obs": "OBS-032",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "secret-op-type",
   "anchor": "security-taint-and-restricted-data.md:735",
   "bucket": "secret",
   "status": "answered",
   "question": "§4.2 row 14 — `type(secret)`.",
   "expect": "Allowed and returns the REAL type. This is §4.3's Trap 1: type() passes, then the comparison blows up. Record the returned string — it also tells us what kind of secret the cooldown source hands out.",
   "answered": {
    "run": "2026-08-05 13:59:03",
    "version": "0.2.0",
    "obs": "OBS-033",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "serializer-secret",
   "anchor": "libraries-and-ecosystem.md:471",
   "also": [
    "libraries-and-ecosystem.md:792"
   ],
   "bucket": "call",
   "status": "built",
   "question": "What happens when a Secret Value reaches AceSerializer-3.0 / LibSerialize / LibDeflate? None of the three tests for secrets; AceSerializer's type dispatch ends in `error(MAJOR..\": Cannot serialize a value of type '\"..t..\"'\")`.",
   "expect": "AceSerializer sees the real type via `type()` (row 14) and takes a normal branch, then errors inside it on a concat/format/length — so the observed failure is a confusing message from the wrong place. Deferred from pass 1 because it needs the three libraries VENDORED into the lab; the §4.2 rows measure the underlying operations first, which is most of the answer.",
   "blocked_on": "LibStub loaded"
  },
  {
   "id": "register-all-then-unregister",
   "anchor": "api-events-and-discovery.md:194",
   "also": [
    "api-events-and-discovery.md:1029"
   ],
   "bucket": "event",
   "status": "answered",
   "question": "Can `RegisterAllEvents` be combined with `UnregisterEvent`? The wiki says no; Blizzard's own Event Trace does exactly that (`self:RegisterAllEvents()` at :107, `self:UnregisterEvent(\"ADDON_LOADED\")` at :702). One of the two is wrong.",
   "expect": "Needs a scratch frame plus observation over time — register all, unregister one, count arrivals of that one. Bucket 2.",
   "note": "Carries a PAIRED CONTROL (SPELL_UPDATE_USABLE, registered and never unregistered) beside the target. If both read 0 the test returns measured=false — a zero with a dead control proves nothing about unregistration.",
   "answered": {
    "run": "2026-08-05 14:32:09",
    "version": "0.2.0",
    "obs": "OBS-038",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "synchronous-event-semantics",
   "anchor": "api-events-and-discovery.md:279",
   "bucket": "event",
   "status": "built",
   "question": "What do the `SynchronousEvent` / `unique` flags mean? Neither string is documented anywhere reachable — not in the generated docs' Documentation field, not in Blizzard_APIDocumentation, not in Blizzard_EventTrace, and the wiki has no page.",
   "expect": "The natural reading (synchronous = dispatched inline; non-synchronous = queued; unique = coalesced to at most one queued instance) is inference, not evidence. Testing it needs firing an event from a known call frame and timestamping the handler — bucket 2, and it may not be reachable from an addon at all.",
   "note": "BUILT but it can only ever report measured=false for the flag question, and that is the honest state, not a stub. Two independent blockers: no runtime predicate exposes SynchronousEvent/UniqueEvent the way C_EventUtils.IsCallbackEvent exposes CallbackEvent, so even a positive observation could not be tied to a flag; and no event is raisable from a known call frame without writing client state (SetCVar/LoadAddOn) or a server round trip, where latency is indistinguishable from queueing. What it DOES measure is the C_EventUtils namespace — if a patch ever adds such a predicate, this test finds it."
  },
  {
   "id": "onupdate-blocked-by-hidden",
   "anchor": "api-events-and-discovery.md:556",
   "bucket": "event",
   "status": "answered",
   "question": "Is `OnUpdate` really blocked by hiding a frame or its parent?",
   "expect": "Tier 2 and dated; oUF guards on `self:IsVisible()` independently, which is consistent but not proof. Scratch frame + tick counter across a Hide/Show cycle.",
   "answered": {
    "run": "2026-08-05 14:32:09",
    "version": "0.2.0",
    "obs": "OBS-039",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "event-callback-nil-owner",
   "anchor": "api-events-and-discovery.md:1259",
   "bucket": "event",
   "status": "built",
   "question": "Is the leading `_nilOwner` argument to a RegisterEventCallback handler a contract, or six consistent conventions? Is it ever non-nil?",
   "expect": "The generated docs declare `EventCallbackType` with NO fields at all, so there is no spec to read. Six shipped call sites all discard a leading arg. Needs registering a callback and inspecting arg 1."
  },
  {
   "id": "reload-addons-unloading-payload",
   "anchor": "anatomy-and-runtime.md:883",
   "bucket": "life",
   "status": "parked",
   "question": "Does `ADDONS_UNLOADING` carry `closingClient = false` on a /reload?",
   "expect": "Tier 1 supplies only the payload shape; that a reload passes false is inference FROM THE FIELD NAME. Needs a logout/reload observation recorded into SavedVariables — pass 3's journal.",
   "blocked_on": null
  },
  {
   "id": "reload-state-preserved",
   "anchor": "anatomy-and-runtime.md:904",
   "bucket": "life",
   "status": "parked",
   "question": "What state does a /reload preserve vs discard — is the Lua state fully destroyed or recycled?",
   "expect": "No Tier-1 or Tier-2 statement exists. Behaviourally addon chunks re-execute (ADDON_LOADED fires again). Testable by stamping a non-saved global and looking for it after reload — pass 3's journal.",
   "blocked_on": null
  },
  {
   "id": "logout-event-order",
   "anchor": "state-persistence-and-communication.md:275",
   "bucket": "life",
   "status": "answered",
   "question": "What is the real order of PLAYER_LEAVING_WORLD → PLAYER_LOGOUT → ADDONS_UNLOADING, and does it differ on a crash/Alt-F4?",
   "expect": "The wiki carries its own {{fact}} tags on both bullets, i.e. it does not vouch for the ordering. Needs the pass-3 journal (record each event with an incrementing sequence number, read it back next session). The crash half is not testable politely.",
   "note": "PLAYER_LEAVING_WORLD also fires on every zone change, so early journal lines may be zoning rather than logout. Each line names its own event and carries GetTime(); the tail is the readable part. Deliberately unfiltered — filtering would discard the only evidence of what actually fired. Cannot see a crash or Alt-F4 at all.",
   "answered": {
    "run": "2026-08-05 14:31:51",
    "version": "0.2.0",
    "obs": "OBS-048",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "fontstring-sublevel-arg",
   "anchor": "frames-textures-animation.md:223",
   "bucket": "frame",
   "status": "answered",
   "question": "Does `CreateFontString` accept a fourth `subLevel` argument, or is it silently discarded? Blizzard's own pool code passes four against a documented three-arg signature.",
   "expect": "Create two FontStrings on the same frame and draw layer with different subLevel values and see whether stacking order differs. Bucket 2.",
   "answered": {
    "run": "2026-08-05 14:31:51",
    "version": "0.2.0",
    "obs": "OBS-041",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "frame-strata-parent",
   "anchor": "frames-textures-animation.md:485",
   "bucket": "frame",
   "status": "answered",
   "question": "What does frameStrata `PARENT` actually do? It is the XSD default and is never written explicitly anywhere in the shipped corpus.",
   "expect": "Tier 1 gives the value and the default and nothing more. The dropped gloss ('inherit from parent, not a z-band') was inference from the name.",
   "answered": {
    "run": "2026-08-05 14:31:51",
    "version": "0.2.0",
    "obs": "OBS-042",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "frame-level-arithmetic",
   "anchor": "frames-textures-animation.md:513",
   "bucket": "frame",
   "status": "answered",
   "question": "Do frame levels run 0–10000, does a child default to one level above its parent, and does moving a parent below 0 clamp to 10000?",
   "expect": "All three are Tier 2 only. Cheap once a scratch frame exists — create parent/child, read GetFrameLevel, then SetFrameLevel(0) on the parent and re-read.",
   "answered": {
    "run": "2026-08-05 14:31:51",
    "version": "0.2.0",
    "obs": "OBS-043",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "draw-layer-z-order",
   "anchor": "frames-textures-animation.md:606",
   "bucket": "frame",
   "status": "answered",
   "question": "Is the XSD's listing order of the five draw layers actually the z-order, and does HIGHLIGHT have special mouse-over semantics?",
   "expect": "Universally believed, matches the names, and stated at Tier 1 nowhere. Needs two overlapping textures and a visual read — the one pass-1 bucket that a SavedVariables record cannot capture on its own.",
   "note": "ANSWERED by eyeball 2026-08-05: OVERLAY draws in front of ARTWORK. ⚠ Five verdicts are on record and they disagree — the first four are from v0.2.0, whose pooled verdict buttons kept a closure over the FIRST question's def and so recorded every answer against this id regardless of which question was on screen (fixed in v0.2.1). Latest-wins gives the correct verdict; the `asked` field recorded beside each verdict is what exposed the mis-binding. Only the ARTWORK/OVERLAY pair was shown, so the full five-way order is still inference from one adjacent pair.",
   "answered": {
    "run": "2026-08-05 15:01:34",
    "version": "0.2.1",
    "obs": "OBS-036",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "texture-source-exclusivity",
   "anchor": "frames-textures-animation.md:644",
   "bucket": "frame",
   "status": "answered",
   "question": "Are SetTexture / SetAtlas / SetColorTexture mutually exclusive — does exactly one 'win'? `SetTexture(...)` then `SetAtlas(...)`, read GetTexture()/GetAtlas(), then the reverse order.",
   "expect": "The near-universal mental model, consistent with GetTexture returning nil after SetAtlas, and cited at no tier.",
   "answered": {
    "run": "2026-08-05 14:31:51",
    "version": "0.2.0",
    "obs": "OBS-044",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "vertexcolor-vs-gradient",
   "anchor": "frames-textures-animation.md:769",
   "bucket": "frame",
   "status": "answered",
   "question": "Do SetVertexColor and SetGradient write the same storage? White 8x8 texture, SetVertexColor(1,0,0), SetGradient(HORIZONTAL, yellow, blue), GetVertexColor(); then reverse.",
   "expect": "README §6 calls this the most consequential unresolved gap in the whole subtree, and the workspace has already observed it once — todo/addon-engineering.md records that SetGradient RESETS the vertex colour to white. That prior observation makes this a confirm-and-cite, not a discovery.",
   "answered": {
    "run": "2026-08-05 14:31:51",
    "version": "0.2.0",
    "obs": "OBS-045",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "settextcolor-vs-getvertexcolor",
   "anchor": "frames-textures-animation.md:811",
   "bucket": "frame",
   "status": "answered",
   "question": "Do FontString:SetTextColor and Region:GetVertexColor share storage? `fs:SetTextColor(1,0,0)` then `fs:GetVertexColor()`, and the reverse.",
   "expect": "'Identical annotations ⇒ identical storage' is inference. The two wiki pages say nothing about the relationship, and one of them was effectively abandoned in 2021.",
   "answered": {
    "run": "2026-08-05 14:31:51",
    "version": "0.2.0",
    "obs": "OBS-046",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "anim-restore-without-final",
   "anchor": "frames-textures-animation.md:1111",
   "bucket": "anim",
   "status": "answered",
   "question": "What does an alpha or vertex-colour animation do to the underlying value when it stops WITHOUT `setToFinalAlpha`?",
   "expect": "The attribute's existence implies restoration, but that is inference and is stated at no tier. Needs an AnimationGroup and a read after OnFinished.",
   "note": "Needs TWO runs. The Stop-path numbers land on the first; the natural-finish numbers come from a persistent arm whose OnFinished fires after `run` has already returned, so a later run reads them. Until then it reports finishMeasured=false with a why — never a stand-in value.",
   "answered": {
    "run": "2026-08-05 14:31:51",
    "version": "0.2.0",
    "obs": "OBS-047",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "xml-onload-varargs",
   "anchor": "anatomy-and-runtime.md:603",
   "bucket": "xml",
   "status": "parked",
   "question": "Does an XML `<OnLoad>` body receive the addon-table varargs? Of 2123 shipped `<OnLoad` occurrences, 6 have `...` nearby and all six forward event/callback varargs — but that is four distinct sites, one of them dead code.",
   "expect": "An argument from silence, not evidence; the KB explicitly declines to assume either way. Needs the lab to ship an XML file.",
   "blocked_on": "XML authoring in the lab"
  },
  {
   "id": "addon-declared-intrinsic",
   "anchor": "frames-textures-animation.md:367",
   "bucket": "xml",
   "status": "parked",
   "question": "Can an addon declare an `intrinsic` in XML and then use it as a `CreateFrame` frameType? The XSD puts `intrinsic` in the shared FrameAttributes group, so an addon CAN write it.",
   "expect": "No Tier-1/Tier-2 statement that it becomes usable, and zero of the seven surveyed addon clones do it. Needs the lab to ship an XML file.",
   "blocked_on": "XML authoring in the lab"
  },
  {
   "id": "scoped-modifier-for-addons",
   "anchor": "module-architecture.md:854",
   "also": [
    "module-architecture.md:1010"
   ],
   "bucket": "xml",
   "status": "parked",
   "question": "May an addon use `<ScopedModifier>` (forbidden / scriptsUseGivenEnv / addToSecureEnv / hideFromGlobalEnv / fullLockdown), or is it silently ignored?",
   "expect": "In UI.xsd (Tier 1), used by 20 Blizzard XML files and by 0 of the surveyed addons and 0 third-party XML files on the live install. Needs the lab to ship an XML file.",
   "blocked_on": "XML authoring in the lab"
  },
  {
   "id": "xml-color-element-target",
   "anchor": "frames-textures-animation.md:856",
   "bucket": "xml",
   "status": "parked",
   "question": "Does an XML `<Color>` element compile to SetVertexColor or to SetColorTexture? Load a texture from XML with both `file` and `<Color>`, then read GetTexture() and GetVertexColor().",
   "expect": "Not stated by UI.xsd or the wiki. The evidence rules out plain SetColorTexture replacement but does not distinguish the two remaining readings. Needs the lab to ship an XML file.",
   "blocked_on": "XML authoring in the lab"
  },
  {
   "id": "toc-optional-deps-absent",
   "anchor": "anatomy-and-runtime.md:449",
   "bucket": "toc",
   "status": "parked",
   "question": "Does an absent `## OptionalDeps` entry block the load? The wiki says OptionalDeps names addons that 'should load first if available' but does NOT say an absent one is harmless.",
   "expect": "Harmless — the natural reading of 'if available', with no counter-example in the shipped corpus, held as [unverified]. Needs a generated sibling addon naming a nonexistent optional dep.",
   "blocked_on": "a generator that writes sibling addons (one .toc variant per test)"
  },
  {
   "id": "toc-lua-error-scope",
   "anchor": "anatomy-and-runtime.md:539",
   "bucket": "toc",
   "status": "parked",
   "question": "Does a Lua error inside one .toc-listed file abort only that file, or the whole addon's remaining files?",
   "expect": "Not established at any tier — the generated docs give only DoesAddOnHaveLoadError with no semantics, and both wiki pages are silent. Needs a sibling addon with a deliberately broken middle file and a marker global set by the last file.",
   "blocked_on": "a generator that writes sibling addons (one .toc variant per test)"
  },
  {
   "id": "toc-dependency-transitivity",
   "anchor": "anatomy-and-runtime.md:1200",
   "bucket": "toc",
   "status": "parked",
   "question": "Is the client's login-time loader transitive over dependencies? Blizzard's Lua demand-load helper walks them recursively, but that proves only the HELPER is transitive.",
   "expect": "No Tier-1 or Tier-2 statement of engine-level transitivity found. Needs a three-addon chain A→B→C.",
   "blocked_on": "a generator that writes sibling addons (one .toc variant per test)"
  },
  {
   "id": "toc-load-environment-tags",
   "anchor": "module-architecture.md:824",
   "also": [
    "module-architecture.md:1006"
   ],
   "bucket": "toc",
   "status": "parked",
   "question": "May a third-party addon use `## LoadIntoEnvironment` / `## AllowLoadEnvironment`? Both are observed in Blizzard .toc files and absent from the wiki's TOC format page, with 0 uses across the install's 147 third-party .toc files.",
   "expect": "Treated as Blizzard-internal until proven otherwise. Needs a sibling addon setting the tag.",
   "blocked_on": "a generator that writes sibling addons (one .toc variant per test)"
  },
  {
   "id": "toc-secure-tag",
   "anchor": "module-architecture.md:845",
   "also": [
    "module-architecture.md:1015"
   ],
   "bucket": "toc",
   "status": "parked",
   "question": "Is a third-party `## Secure` ignored, honoured, or rejected? 5 shipped Blizzard tocs set it, 0 of 147 third-party ones do, and the wiki does not document the tag at all — it is undocumented, NOT documented-as-restricted.",
   "expect": "Unknown; that is the finding to make. Needs a sibling addon setting it.",
   "blocked_on": "a generator that writes sibling addons (one .toc variant per test)"
  },
  {
   "id": "toc-parser-case-sensitivity",
   "anchor": "module-architecture.md:824",
   "bucket": "toc",
   "status": "parked",
   "question": "Is the .toc directive parser case-insensitive?",
   "expect": "Not established. Needs a sibling addon with `## interface:` / `## SAVEDVARIABLES:` in odd casing.",
   "blocked_on": "a generator that writes sibling addons (one .toc variant per test)"
  },
  {
   "id": "toc-file-order-guarantee",
   "anchor": "module-architecture.md:1025",
   "bucket": "toc",
   "status": "parked",
   "question": "Are .toc files loaded strictly top-to-bottom? The rule is Tier 2 only, corroborated only by Blizzard's own tocs being internally consistent with it.",
   "expect": "True. Trivially testable with a sibling addon whose files append their own name to a table — and worth doing precisely because everything in the module-architecture chapter rests on it.",
   "blocked_on": "a generator that writes sibling addons (one .toc variant per test)"
  },
  {
   "id": "unit-token-string-vs-enum",
   "anchor": "api-events-and-discovery.md:174",
   "bucket": "call",
   "status": "not-answerable",
   "question": "WHY is a string accepted where an enum is declared — does the C layer map the token string onto the enum, or does the doc generator merely label unit-token parameters with that type name?",
   "expect": "NOT ANSWERABLE from Lua. Both readings produce identical observable behaviour, so no call can distinguish them; only Blizzard source or a statement could. The KB's handling is already right: use the string form (that is what ships), assert no mechanism."
  },
  {
   "id": "taintlog-level-5",
   "anchor": "security-taint-and-restricted-data.md:467",
   "also": [
    "security-taint-and-restricted-data.md:1869"
   ],
   "bucket": "call",
   "status": "not-answerable",
   "question": "What does `taintLog` level 5 (added 12.0.1, per the wiki) log? The BlizzardInterfaceResources CVar description stops at level 4.",
   "expect": "NOT ANSWERABLE BY AN ADDON. Needs `/console taintLog 1`, a full client restart, and then reading `_retail_/Logs/taint.log` OFF DISK — three things outside a Lua addon's reach. Answerable by a human plus a file read, and worth doing that way; just not by ClientLab.",
   "blocked_on": "a desktop file read"
  },
  {
   "id": "savedvars-secret-comment",
   "anchor": "state-persistence-and-communication.md:355",
   "bucket": "call",
   "status": "not-answerable",
   "question": "When a secret is written to SavedVariables, does the file really get 'a comment' alongside the nil?",
   "expect": "The COMMENT half is not answerable by an addon — it needs reading the written file off disk, which is a wowkb job, not a Lua one. The other half — 'secrets become nil' — IS addon-testable via a two-session round trip (write a secret to the DB, /reload, read it back), and is recorded as `savedvars-secret-roundtrip` below.",
   "blocked_on": "a desktop file read"
  },
  {
   "id": "savedvars-secret-roundtrip",
   "anchor": "state-persistence-and-communication.md:934",
   "bucket": "life",
   "status": "answered",
   "question": "Does a Secret Value stored in SavedVariables come back as nil after a /reload?",
   "expect": "Traceable only to a Discord blue post archived on the wiki; the KB could not produce it locally because no SavedVariable on this install held a secret. A two-session round trip settles it. PASS 3 — it is the first test that needs the journal (write in session N, assert in session N+1), so it validates that machinery.",
   "note": "Two-phase (write -> /reload -> read back) AND QUARANTINED into a second SavedVariable, ClientLabScratchDB: NaN/infinity may serialise to something the loader rejects, a self-referencing table may hang the client at logout, and a Secret Value reaching the writer is the question itself. Any of those can take the whole file, and ClientLabDB holds the `runs` capture ring. Does NOT auto-re-arm — the risky bytes sit on disk for exactly one logout. needs=\"secret\", so BOTH phases must be flown in combat: pull -> /clab run (arms) -> /reload -> pull -> /clab run (reads back).",
   "answered": {
    "run": "2026-08-05 14:35:51",
    "version": "0.2.0",
    "obs": "OBS-049",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "savedvars-number-precision",
   "anchor": "state-persistence-and-communication.md:925",
   "bucket": "life",
   "status": "answered",
   "question": "How does the SavedVariables writer format numbers — what precision, and what does it do with NaN / inf?",
   "expect": "Unverified at every tier; the writer is C, not shipped Lua, so §1.5 is entirely inferred from output files. Write known values, /reload, read back.",
   "note": "Two-phase (write -> /reload -> read back) AND QUARANTINED into a second SavedVariable, ClientLabScratchDB: NaN/infinity may serialise to something the loader rejects, a self-referencing table may hang the client at logout, and a Secret Value reaching the writer is the question itself. Any of those can take the whole file, and ClientLabDB holds the `runs` capture ring. Does NOT auto-re-arm — the risky bytes sit on disk for exactly one logout.",
   "answered": {
    "run": "2026-08-05 14:35:51",
    "version": "0.2.0",
    "obs": "OBS-050",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "savedvars-table-cycles",
   "anchor": "state-persistence-and-communication.md:925",
   "bucket": "life",
   "status": "answered",
   "question": "What does the SavedVariables writer do with a table cycle, and is there a depth limit?",
   "expect": "Unverified at every tier. Risky to test — a hang or a corrupt file is a plausible outcome — so it wants its own throwaway SavedVariable, not ClientLabDB.",
   "note": "Two-phase (write -> /reload -> read back) AND QUARANTINED into a second SavedVariable, ClientLabScratchDB: NaN/infinity may serialise to something the loader rejects, a self-referencing table may hang the client at logout, and a Secret Value reaching the writer is the question itself. Any of those can take the whole file, and ClientLabDB holds the `runs` capture ring. Does NOT auto-re-arm — the risky bytes sit on disk for exactly one logout.",
   "answered": {
    "run": "2026-08-05 14:35:51",
    "version": "0.2.0",
    "obs": "OBS-051",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "savedvars-key-ordering",
   "anchor": "state-persistence-and-communication.md:925",
   "bucket": "life",
   "status": "answered",
   "question": "Is SavedVariables key ordering stable across writes?",
   "expect": "Unverified. Matters for anything diffing the file between sessions — which wowkb.cdmp and wowkb.lab both do.",
   "answered": {
    "run": "2026-08-05 14:31:51",
    "version": "0.2.0",
    "obs": "OBS-052",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "savedvars-bak-semantics",
   "anchor": "state-persistence-and-communication.md:929",
   "bucket": "life",
   "status": "parked",
   "question": "Is the `.bak` file a rename or a copy, and when is it written relative to the main file?",
   "expect": "Undocumented at Tier 1 and Tier 2; WoWUIBugs #241 is Tier-2 evidence it is not a reliable recovery mechanism. Half of this is a disk observation, not an addon one.",
   "blocked_on": "a desktop file read"
  },
  {
   "id": "addon-message-size-limit",
   "anchor": "state-persistence-and-communication.md:935",
   "bucket": "call",
   "status": "answered",
   "question": "Is the 255-byte addon-message body limit real, and what happens on overflow — truncation, rejection, or an error?",
   "expect": "Same Tier-2/Tier-3-only footing as the prefix limit. Deferred past the prefix test because sending actually puts traffic on a channel, which the prefix registration does not.",
   "note": "Records the RETURN CODE as a named enum, never truthiness. The delivered-length half rests on an unverified assumption that the client echoes CHAT_MSG_ADDON to the sender on a self-whisper; if it does not, truncation-vs-rejection stays open and the test reports 'nothing received yet' rather than anything that could read as a measurement.",
   "answered": {
    "run": "2026-08-05 14:31:51",
    "version": "0.2.0",
    "obs": "OBS-054",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "addon-message-throttle",
   "anchor": "state-persistence-and-communication.md:938",
   "bucket": "call",
   "status": "parked",
   "question": "Are the addon-message throttle numbers (10 allowance, 1/s regen) real?",
   "expect": "Tier 2 only, and the same source says the server may change them at any time — the KB's own instruction is DO NOT ENCODE THEM. Measuring them would produce a number with a shelf life, so this is recorded mainly so a future session does not think it was missed.",
   "blocked_on": "a second client"
  },
  {
   "id": "battlenet-senddata-limits",
   "anchor": "state-persistence-and-communication.md:940",
   "bucket": "call",
   "status": "parked",
   "question": "Are there length or rate limits on `C_BattleNet.SendGameData`?",
   "expect": "No limits documented at any tier. Testing needs a second Battle.net-connected client, which makes it much more expensive than it looks.",
   "blocked_on": "a second client"
  },
  {
   "id": "encoding-util-error-semantics",
   "anchor": "state-persistence-and-communication.md:942",
   "bucket": "call",
   "status": "answered",
   "question": "Under what conditions do the six `MayReturnNothing` functions of `C_EncodingUtil` return nothing?",
   "expect": "Nothing states the conditions. Pure call-and-record once someone enumerates plausible bad inputs — a good pass-2 addition, cheap and self-contained.",
   "answered": {
    "run": "2026-08-05 14:31:51",
    "version": "0.2.0",
    "obs": "OBS-055",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "event-dispatch-order-between-frames",
   "anchor": "api-events-and-discovery.md:1025",
   "bucket": "event",
   "status": "answered",
   "question": "In what order do two frames registered for the same event receive it — registration order, creation order, or unspecified?",
   "expect": "No Tier-1 statement, and the wiki's account is explicitly disclaimed by the wiki itself. Two scratch frames plus a shared counter. Bucket 2.",
   "note": "Frames are CREATED ABCD but REGISTERED ABDC, so creation order and registration order disagree on the last pair and the observed order discriminates between them. A same-order design would have been consistent with both hypotheses and measured nothing.",
   "answered": {
    "run": "2026-08-05 14:32:09",
    "version": "0.2.0",
    "obs": "OBS-040",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "savedvariables-first-ordering",
   "anchor": "state-persistence-and-communication.md:147",
   "bucket": "life",
   "status": "answered",
   "question": "Under `## LoadSavedVariablesFirst`, when do ADDON_LOADED and SAVED_VARIABLES_TOO_LARGE fire relative to the .toc's last file?",
   "expect": "The only statement is a PROPOSAL in a feature request, not a shipped-behaviour statement. Needs the lab's own .toc to set the tag, which changes the lab's own load behaviour — so it belongs with the W1d container work rather than with the in-process tests.",
   "note": "Measures the DEFAULT branch only — the lab's .toc does not set LoadSavedVariablesFirst, so this answers 'when is the global populated WITHOUT the directive', which is the unmeasured half of the wiki's claim. The directive's own behaviour stays W1d (it needs a sibling addon that declares it).",
   "answered": {
    "run": "2026-08-05 14:31:51",
    "version": "0.2.0",
    "obs": "OBS-053",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "secret-aspectless-pixel-moved",
   "anchor": "security-taint-and-restricted-data.md:1282",
   "also": [
    "security-taint-and-restricted-data.md:1284",
    "security-taint-and-restricted-data.md:1461"
   ],
   "bucket": "secret",
   "status": "built",
   "question": "Do the ASPECT-LESS secret sinks actually move a pixel? `Texture:SetAtlas` and `AnimVertexColor:SetStartColor`/`SetEndColor` accept a Secret Value, declare no `SecretArgumentsAddAspect`, expose no getter, and leave the anchor chain clean — so 'accepted, nothing observable changed' is the strongest statement any instrument can make. Only an eyeball can close it. (`SetTexture` REFUSES and `SetColorTexture` POISONS the anchor chain — both are settled and are NOT part of this question.)",
   "expect": "Set each from a known secret whose value is inferable from context (e.g. a boolean-ish count at a threshold), and LOOK. Either the atlas/colour visibly tracks the secret — in which case these are real display channels with no readback, and the KB's channel table gains two rows — or nothing moves, and 'accepted' means the call was silently dropped. ⚠ There is no programmatic oracle by construction: do not build one, and do not accept an INERT verdict as evidence either way.",
   "blocked_on": "a secret that is itself a valid atlas name",
   "note": "STILL OPEN after a visual check on 2026-08-05, deliberately not drained. The SetAtlas half was shown as three cells (valid name / invalid name / secret string) and the person reported the secret cell INDISTINGUISHABLE FROM THE INVALID one — so the blankness cannot be attributed to secrecy, because an atlas name is a string and the only secret string obtainable (a concatenation, §4.2 row 4) is never a valid atlas name. The control is what revealed that; without it the run would have looked like a clean negative. Settling SetAtlas needs a secret that is ITSELF a valid atlas name and no such source is known. The AnimVertexColor SetStartColor/SetEndColor half is UNATTEMPTED and is the live one — those take colours, not names, so the category problem does not apply."
  },
  {
   "id": "toc-category-grouping",
   "anchor": "anatomy-and-runtime.md:248",
   "bucket": "call",
   "status": "parked",
   "question": "Does `## Category:` actually group addons in the client's AddOn list? The directive is proven by THIRD-PARTY practice (EllesmereUI sets it, colour escapes and all) but Blizzard's own corpus is a non-example — 1 of 346 shipped tocs.",
   "expect": "Container-level: needs sibling addons written with differing `## Category:` values, so the lab cannot test it from inside its own .toc (W1d). Half of it is also VISUAL — whether the list groups — which only an eyeball closes. The addon-testable half is whether `C_AddOns.GetAddOnMetadata(name, 'Category')` reads the raw string back including the `|cff...|r` escape.",
   "blocked_on": "a generator that writes sibling addons (one .toc variant per test)"
  },
  {
   "id": "cdm-auradatacached-plain-in-combat",
   "anchor": "cooldown-manager.md:748",
   "also": [
    "mined-pending-verification.md:48"
   ],
   "bucket": "secret",
   "status": "answered",
   "question": "⭐ Is `item.auraDataCached` / `item:GetAuraDataCached()` on a Cooldown Manager item frame PLAIN in combat, or secret like every sibling field? Every other aura-derived field on that frame measured secret; this one is unmeasured.",
   "expect": "UNKNOWN, and it is the highest-value open question in the subtree. If the record is plain in combat it carries `expirationTime`/`duration`, which makes the in-combat DoT-remaining read that `cooldown-manager.md` §5.1 and §7 BOTH declare unanswerable already available on the frame. Method: `ns.ClassOf` the record itself BEFORE indexing it (the container may be secret independently of its fields, per the 12.1.0 `UNIT_AURA` note), then class-check each field. Needs a real pull with a bound aura — out of combat proves nothing, since every sibling reads fine there too.",
   "answered": {
    "run": "2026-08-05 13:59:03",
    "version": "0.2.0",
    "obs": "OBS-019",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "secret-compare-to-nil-permitted",
   "anchor": "security-taint-and-restricted-data.md:1514",
   "bucket": "secret",
   "status": "answered",
   "question": "Is `x == nil` genuinely PERMITTED on a Secret Value, while `x == <number>` throws? Bare `==` against a number killed a refresh loop the instant combat started `[client 2026-08-04]`, but nil-guards have run on secrets every sample of every capture without raising.",
   "expect": "The asymmetry is consistent with the model — comparing to nil leaks nothing not already knowable — but it is inferred from two data points and NO Tier-1 statement says so. Test both directions against a known secret inside `pcall`: `s == nil`, `nil == s`, `s ~= nil`, and a control `s == 0` that must throw. ⚠ A pass does NOT license comparing to nil in product code — house rule stands: class-check first, branch on the class. This question exists to make the KB's claim honest, not to change the practice.",
   "answered": {
    "run": "2026-08-05 13:59:03",
    "version": "0.2.0",
    "obs": "OBS-020",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "traitconfig-fires-on-hero-swap",
   "anchor": "../../knowledge/_meta/kb-inbox.md:140",
   "bucket": "call",
   "status": "built",
   "question": "Does `TRAIT_CONFIG_UPDATED` fire on a HERO-TREE swap (which changes the build without changing the spec), and how many times per loadout swap? `PLAYER_SPECIALIZATION_CHANGED` demonstrably does not, so a hero-tree cache keyed to it goes stale.",
   "expect": "Reasoned from the event's purpose, NOT observed — that is why it is here. Register both events plus `TRAIT_CONFIG_UPDATED`, log every firing with `C_ClassTalents.GetActiveHeroTalentSpec()` beside it, then swap hero tree without swapping spec. Expect TRAIT_CONFIG_UPDATED n>1 and PLAYER_SPECIALIZATION_CHANGED n=0. The COUNT matters as much as the fact: anything it triggers must be idempotent and must not re-announce an unchanged answer. ⚠ Anchor is outside knowledge/addon-dev/ — the finding is parked in the game KB's inbox because it has no home topic file yet (that placement is itself an open KB-structure call).",
   "note": "Observational: an always-on listener installed at file load tallies PER-EVENT counts, so a reading requires an actual hero-tree swap during the session. A zero count reports measured=false, never 'it does not fire'."
  },
  {
   "id": "cdm-aura-duration-object-chain",
   "anchor": "cooldown-manager.md:748",
   "also": [
    "security-taint-and-restricted-data.md:1323"
   ],
   "bucket": "secret",
   "status": "answered",
   "question": "Does the CDM item frame's PLAIN `auraInstanceID` reach `C_UnitAuras.GetAuraDuration`, yielding a `LuaDurationObject` that carries the sealed aura timing for display? Measured on a BUFF row and a DEBUFF row separately.",
   "expect": "UNKNOWN, and it is the follow-on to `cdm-auradatacached-plain-in-combat`. That measurement killed the direct read (`expirationTime`/`duration`/`timeMod`/`applications` all secret) but left `auraInstanceID` PLAIN — and a secret can never be fed INTO a curve or a duration, since every sink is `AllowedWhenUntainted` and all four negative controls refused `[client 2026-08-04]`. The sanctioned route is the reverse: an API returns an object already carrying the secret internally. §4.8.1 measured `C_UnitAuras.GetAuraDuration` behaving identically to `GetSpellCooldownDuration`, so IF the plain instance id is the key it accepts, the CDM frame supplies a legal in-combat aura timer for display — which is a capability, not a footnote. ⚠ The SIGNATURE is unverified: both arities are tried and the error is recorded, because an argument-count complaint names the real shape. ⚠ `HasSecretValues()` is `ReturnsNeverSecret`, so its VALUE (not just its class) is the one real readback — expect true in combat. ⚠ `GetRemainingDuration` returning a PLAIN number in combat would be a hole in the seal, not a convenience; do not treat it as one. ⚠ The duration sinks are ASPECT-LESS, so this test can prove the object was obtained and carries secrets, and can NEVER prove a pixel moved — that half needs an eyeball.",
   "note": "Destruction is the target spec: it puts a DoT on the target (Immolate 133441 / Wither 164597 -> auraDataUnit \"target\") and carries player-side buffs (Backdraft, Malevolence, Conflagration of Chaos -> \"player\"), so one dummy pull exercises both sides. Rows are bucketed by `auraDataUnit`, never by spell id, so the split stays spec-agnostic.",
   "answered": {
    "run": "2026-08-05 14:35:32",
    "version": "0.2.0",
    "obs": "OBS-034",
    "tag": "[client 2026-08-05]"
   }
  },
  {
   "id": "cdm-aura-duration-renders",
   "anchor": "cooldown-manager.md:748",
   "also": [
    "security-taint-and-restricted-data.md:1323"
   ],
   "bucket": "secret",
   "status": "answered",
   "question": "Does a secret-bearing aura duration obtained via `C_UnitAuras.GetAuraDuration` actually DRAW when fed to `StatusBar:SetTimerDuration` — for a player buff and a target debuff alike?",
   "expect": "UNKNOWN, and unknowable by instrument: all three duration sinks declare NO `SecretArgumentsAddAspect`, so there is no readback of any kind and 'the call was accepted' is not evidence a pixel moved. §4.8.1 states it outright — on an aspect-less channel the only oracle is an eyeball. `cdm-aura-duration-object-chain` already proved the object is obtained on both sides and that `HasSecretValues()` is true, so what is left is purely whether it renders. ⚠ A PLAIN CONTROL duration is shown in the same frame: without it 'the bar is empty' cannot be told from 'this widget never animates'. ⚠ `SetMinMaxValues(0,1)` is called BEFORE the timer on every bar — the known trap is a correctly installed duration drawing at 0 % width, which would read as the secret having been dropped. ⚠ 'control doesn't animate either' is a real option and indicts the STIMULUS, not the channel.",
   "note": "VISUAL CHECK (ns.Ask.Register), no ns.Test — by construction there is no programmatic oracle. Needs a live bound aura in combat; the stimulus refuses to draw and says so when none is present, rather than showing empty bars that could be answered by mistake. Destruction gives both sides at once (Immolate on the target, a self-buff on the player).",
   "answered": {
    "run": "2026-08-05 15:01:34",
    "version": "0.2.1",
    "obs": "OBS-035",
    "tag": "[client 2026-08-05]"
   }
  }
 ]
}
