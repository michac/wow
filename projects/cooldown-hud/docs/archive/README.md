# docs/archive — spent milestone working docs

Dated, **spent** working docs from the **M4 milestone push** (M4 → M4.5), moved
here 2026-07-24 when the effort pivoted to **W4** (the audit + refactor, see
`../architecture.md` and `todo/w4-hud-audit.md`). M4 shipped **code-complete**; its
in-game QA (`../qa-pending.md` §7.x) was never run before the W4 refactor superseded
the code it would have verified. Kept for design history, not as live plans.

- `m4-plan.md` — the M4 master plan (movable pane, prereqs, burst-window queue).
- `m4.1-plan.md` — M4.1 phased plan (A→B→C→D).
- `m4.4-plan.md` — the M4.4 HUD pass (5 workstreams A–E).
- `m4.4-feedback.md` — play-test-4 feedback that seeded M4.4.
- `m4.5-plan.md` — the M4.5 tooling track (luacheck / busted / selftest sketch).

**Still live, deliberately NOT archived** (they live one level up in `../`):
- `m4.5-t3-plan.md` — the **collect/assert** governing doctrine, cited by the root
  `CLAUDE.md`, `wowkb.cdmp`, and ClientLab. Load-bearing, not history.
- `m4.5-playtest5-feedback.md` — cited by `wowkb.cdmp` as probe-baseline evidence.

Recover anything here via `git log`/`git show`; nothing points to these as current.
