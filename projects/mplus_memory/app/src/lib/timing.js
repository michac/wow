/** timing.js — shared cast-bar duration. */

// The cast bar is both the timer the player sees and the clock the latency
// grader reads. Drill and Test import this one number so they can't drift.
export const DURATION = 7000;
