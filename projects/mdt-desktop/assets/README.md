# MDT Desktop icon
The motif is a bright, ordered pull route winding across a dark violet dungeon floor plan.
Each icon size is drawn independently so the bold route and three pull nodes remain legible at 16px.
Regenerate with `uv run --with pillow python make_icon.py`.
